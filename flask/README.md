# master controls for smart thermostat project

This project is being done as a learning experience in front end development.

## Requirements

python 3.7+
flask

## Starting up the Server

The flask web server can be started with the following command

``` bash
python wsgi.py
```

